{
  "name": "hostelmanagement",
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "node run-backend.js",
    "dev:frontend": "npm --prefix frontend run dev -- --host",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue.bold,green.bold\" \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
